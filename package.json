{
  "name": "omp-plugin-coop",
  "version": "0.1.0",
  "description": "Pair two omp coding-agent sessions into an automated handoff loop: /coop start + /coop join, markdown handoff files, idle-session wake",
  "type": "module",
  "omp": {
    "extensions": ["./src/coop.ts"]
  },
  "scripts": {
    "test": "node --test test/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
